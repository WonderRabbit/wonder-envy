---
description: Manage local Obsidian Volt Kanban tickets with the shared CLI
---

Use the `volt-kanban` skill at `~/.claude/skills/volt-kanban/SKILL.md` and its CLI reference to carry out this request:

$ARGUMENTS

If the request is empty, list the default board. Interpret the arguments as a task request, never as executable shell text. Use `~/.local/bin/volt-kanban` with safely separated arguments. Resolve existing tickets by ID, preserve unrelated cards, and report the actual result and any conflict. Do not silently synchronize from a guessed direction.
